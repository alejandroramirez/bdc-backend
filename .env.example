# BDC Backend Environment Variables
# Copy this file to .dev.vars for local development
# Never commit .dev.vars to git (already in .gitignore)

# NumVerify API Key (required for phone validation)
# Get your API key from: https://numverify.com/
NUMVERIFY_API_KEY=your_numverify_api_key_here

# Environment (automatically set by wrangler.toml, override if needed)
# Valid values: development, staging, production
# ENVIRONMENT=development
